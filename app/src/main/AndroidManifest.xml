<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QUIZBIN1"
        tools:targetApi="31"
        android:enableOnBackInvokedCallback="true">
        <activity android:name=".ui.user.RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.settings.SettingsActivity" />
        <activity android:name=".ui.settings.TermsActivity" />
        <activity android:name=".ui.settings.EditProfileActivity" />
        <activity android:name=".ui.settings.ChangePasswordActivity" />
        <activity android:name=".ui.semester.SemesterActivity" />
        <activity android:name=".ui.quiz.QuizDoingActivity" />
        <activity android:name=".ui.result.ResultActivity" />


        <activity
            android:name=".ui.user.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>